insertar_button = tk.Button(root, text="Insertar Partido", command=insertar_partido)
insertar_button.grid(row=
insertar_button.grid(row=

insertar_button.grid(row

insertar_button.grid

insertar_button

insertar

insert
4, column=0, columnspan=2)
root.mainloop()